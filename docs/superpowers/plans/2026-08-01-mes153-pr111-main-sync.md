# MES-153 PR #111 Main Synchronization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Merge the latest `origin/main` into PR #111’s head without losing the search/command-palette or newer roster/inbox/chat behavior, run every required gate, and update the existing PR branch.

**Architecture:** Treat `origin/main` as the source of truth for already released platform and CI behavior, then resolve only the five overlapping frontend files as semantic unions. Existing tests from both sides are the first executable contract; any newly discovered behavioral defect gets a failing regression test before its implementation fix.

**Tech Stack:** Git, React 19, TypeScript, Vitest, Playwright, Python 3.12, FastAPI, pytest, PostgreSQL 16, Redis, Alembic, nginx.

## Global Constraints

- Work only from PR #111, the latest `origin/main`, and `docs/specs/features/search-command-palette.md`.
- Preserve authentication, RBAC, favorites, integration ingestion, worker registration, and CI semantics from both sides; stop before push if an authorization conflict is substantive.
- Keep backend and changed-code coverage at or above 90%, and keep frontend global/per-file/changed-code coverage at or above 90%.
- Use strong per-run credentials for every local datastore; expose no datastore port publicly.
- Commit author and committer must both be `cnwenf <cnwenf@outlook.com>` with no co-author trailers.
- Push only to `origin/agent/mesh/d609122f`; do not create another PR or manually trigger CI.

---

### Task 1: Freeze the merge inputs and record the conflict contract

**Files:**
- Create: `docs/superpowers/plans/2026-08-01-mes153-pr111-main-sync.md`
- Read: `docs/specs/features/search-command-palette.md`

**Interfaces:**
- Consumes: PR head `e38319b98c2c8c5902c955a7435d83312e98ac2d` and fetched `origin/main`.
- Produces: a clean task-scoped branch and an exact five-file conflict inventory.

- [ ] **Step 1: Confirm the checkout is clean and the PR head matches the remote head**

Run: `git status --short --branch && git rev-parse HEAD origin/agent/mesh/d609122f`

Expected: no working-tree changes other than this plan and identical PR-head SHAs before the plan commit.

- [ ] **Step 2: Commit the plan with the required identity**

Run: `git add docs/superpowers/plans/2026-08-01-mes153-pr111-main-sync.md && git commit -m "docs(plan): 记录 MES-153 主干同步与全门禁方案"`

Expected: one documentation commit by `cnwenf`, with no co-author trailer.

- [ ] **Step 3: Re-fetch and preview the merge**

Run: `git fetch origin main agent/mesh/d609122f && git merge-tree --write-tree --name-only --messages HEAD origin/main`

Expected: only `frontend/src/App.tsx`, `frontend/src/features/chat/ChatComposer.tsx`, `frontend/src/features/chat/ConversationPanel.tsx`, and the two locale catalogs require manual resolution. If the set expands, inspect and add the new file’s semantic contract before resolving it.

### Task 2: Merge and resolve the five frontend overlaps

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/features/chat/ChatComposer.tsx`
- Modify: `frontend/src/features/chat/ConversationPanel.tsx`
- Modify: `frontend/src/i18n/catalogs/en.json`
- Modify: `frontend/src/i18n/catalogs/zh-CN.json`

**Interfaces:**
- Consumes: canonical route migration and keyboard contracts from PR #111; responsive conversation/run-state and workspace-scoped upload behavior from `main`.
- Produces: one merge commit containing both behavior sets and valid catalog versions.

- [ ] **Step 1: Start the merge without committing**

Run: `git merge --no-ff --no-commit origin/main`

Expected: Git stops with the five previewed content conflicts and no additional unmerged paths.

- [ ] **Step 2: Resolve `App.tsx` as a canonical-route union**

Keep the PR’s workspace-scoped route tree and catch-all `FlatRouteMigration`. The merged route block must include both inbox forms and both chat forms:

```tsx
<Route path="w/:workspaceSlug/inbox" element={<InboxPage />} />
<Route path="w/:workspaceSlug/inbox/:notificationId" element={<InboxPage />} />
<Route path="w/:workspaceSlug/chat" element={<ChatPage />} />
<Route path="w/:workspaceSlug/chat/:sessionId" element={<ChatPage />} />
```

Do not restore the removed flat route tree; `/inbox/:notificationId` and `/chat/:sessionId` compatibility belongs in `FlatRouteMigration`.

- [ ] **Step 3: Resolve `ChatComposer.tsx` as a prop and behavior union**

The merged public props and uploader construction must contain both additions:

```tsx
readonly draftSeed?: { readonly nonce: number; readonly content: string } | null;
readonly workspaceId?: string;
const uploader = useAttachmentUploader({ workspaceId: props.workspaceId });
```

Retain the PR’s textarea ref, draft-seed effect, IME/229 guard, Enter/Shift+Enter/mod+Enter handling, and Escape blur behavior.

- [ ] **Step 4: Resolve `ConversationPanel.tsx` as a UI/data-flow union**

Retain `CHAT_EDIT_LAST_EVENT`, `messagesRef`, and `draftSeed`; retain `Icon`, `IconButton`, `RunStateBadge`, responsive back navigation, and live queued/running feedback. Pass both new props to the composer:

```tsx
<ChatComposer
  onSend={handleSend}
  quoteMessage={quoteMessage}
  onClearQuote={() => setQuoteMessage(null)}
  disabled={isArchived || stream.isStreaming}
  draftSeed={draftSeed}
  workspaceId={workspaceId}
/>
```

- [ ] **Step 5: Resolve both catalogs as key unions and regenerate their hashes**

Keep every message key from both sides, remove all conflict markers, then run from `frontend/`:

Run: `node scripts/add-search-keys.mjs`

Expected: zero newly required search keys and a recomputed eight-character version for each complete merged message map.

- [ ] **Step 6: Validate the resolution before committing**

Run: `git diff --check && git diff --name-only --diff-filter=U && rg -n '^(<<<<<<<|=======|>>>>>>>)' frontend/src`

Expected: all three commands report no unresolved or malformed content.

### Task 3: Run focused red/green checks and close any regressions test-first

**Files:**
- Test: `frontend/src/shell/__tests__/App.test.tsx`
- Test: `frontend/src/workspace/__tests__/flatRoutes.test.tsx`
- Test: `frontend/src/features/chat/__tests__/ChatComposer.test.tsx`
- Test: `frontend/src/features/chat/__tests__/ConversationPanel.runstate.test.tsx`
- Test: `frontend/src/features/board/__tests__/BoardColumns.test.tsx`
- Test: `frontend/src/i18n/__tests__/catalogLoader.test.ts`

**Interfaces:**
- Consumes: the resolved frontend files.
- Produces: executable evidence that both parent behaviors remain reachable.

- [ ] **Step 1: Run type checking and focused tests before any corrective source edit**

Run: `npm run typecheck && npx vitest run src/shell/__tests__/App.test.tsx src/workspace/__tests__/flatRoutes.test.tsx src/features/chat/__tests__/ChatComposer.test.tsx src/features/chat/__tests__/ConversationPanel.runstate.test.tsx src/features/board/__tests__/BoardColumns.test.tsx src/i18n/__tests__/catalogLoader.test.ts`

Expected: all focused contracts pass. If one fails because the semantic union is incomplete, preserve the failing output, add or tighten the smallest regression test when coverage is absent, verify that test fails for the expected reason, make one minimal source fix, and rerun it green.

- [ ] **Step 2: Commit the merge resolution**

Run: `git add -A && git commit -m "merge(main): 同步 MES-87 与批次三并保留 MES-79 契约"`

Expected: a two-parent merge commit by `cnwenf`, no unresolved files, and no co-author trailer.

### Task 4: Execute the complete local quality gate

**Files:**
- Verify: `.github/workflows/backend-ci.yml`
- Verify: `.github/workflows/frontend.yml`
- Verify: `.github/workflows/spec-checks.yml`
- Verify: `frontend/e2e/real-mes79-search.spec.ts`
- Verify: deployment nginx configuration selected by the repository stack.

**Interfaces:**
- Consumes: the committed merged tree and isolated strong-credential test services.
- Produces: fresh lint, coverage, build, migration, API e2e, browser e2e, and nginx evidence.

- [ ] **Step 1: Verify backend lint, lockfiles, full unit/e2e coverage, and supply chain**

Run the exact merged workflow commands: `ruff check backend/src backend/tests`; install from `backend/requirements-dev.lock`; run `pytest --cov=mesh --cov-report=term-missing --cov-fail-under=90` from `backend/` against isolated PostgreSQL 16, Redis, and object storage using strong per-run secrets; regenerate lockfiles only if dependency inputs differ, using the two `uv pip compile` commands documented in `backend/README.md`; run both `pip-audit --strict --progress-spinner off` lockfile audits.

Expected: zero lint/test/audit failures and total backend branch coverage at least 90%.

- [ ] **Step 2: Verify specs and a fresh real migration chain**

Run: `python3 tests/docs/check_event_vocab.py docs/specs && python3 tests/docs/check_roster_entry.py docs/specs && python3 tests/docs/check_semantic_consistency.py docs/specs`

Run `alembic heads` and `alembic upgrade head` against a fresh isolated PostgreSQL 16 database with a strong per-run password.

Expected: all three spec checks pass, Alembic reports one head, and a fresh database reaches that head.

- [ ] **Step 3: Verify frontend quality and coverage**

Run from `frontend/`: `npm ci`, `npm run lint`, `npm run lint:css`, `npm run typecheck`, `npm run test:coverage`, `node scripts/verify-coverage.mjs --base origin/main`, `node scripts/check-evidence-unique.mjs`, `npm run check:contrast`, and `npm run build`.

Expected: zero failures; global, per-file, and changed-code coverage each remain at least 90%; production build succeeds.

- [ ] **Step 4: Verify browser behavior**

Run the normal browser suite and visual regression suite, then run `playwright test -c playwright.mes79.config.ts` against the real application stack and confirm `real-mes79-search.spec.ts` is 5/5. Re-run the board movement arbitration test to confirm Enter executes only the move handler while move mode is active.

Expected: every browser suite passes, MES-79 reports 5/5, and MES-150 remains green.

- [ ] **Step 5: Verify deployment configuration**

Run `nginx -t` using the repository’s production nginx configuration and built frontend artifact.

Expected: syntax and configuration tests succeed.

### Task 5: Independently review, close the race, and update the existing PR

**Files:**
- Review: frozen `origin/main...HEAD` diff after the final merge.

**Interfaces:**
- Consumes: all local gate evidence and the final fetched `origin/main`.
- Produces: reviewed, race-free PR head on `origin/agent/mesh/d609122f`.

- [ ] **Step 1: Run independent code, E2E, and security review**

Use the requesting-code-review workflow with separate static lenses, E2E, security, candidate validation, and final aggregation. Resolve any confirmed defect with a red-green regression cycle and rerun affected gates.

- [ ] **Step 2: Perform the final anti-race fetch**

Run: `git fetch origin main agent/mesh/d609122f && git rev-parse origin/main origin/agent/mesh/d609122f`

Expected: the remote PR head is the expected predecessor and `origin/main` is already an ancestor of `HEAD`. If `main` moved, merge it, repeat conflict resolution and affected/full gates, and fetch again until current.

- [ ] **Step 3: Audit commit identity and trailers**

Run: `git log -1 --format='%an <%ae> | %cn <%ce>%n%B'` and `git log origin/agent/mesh/d609122f..HEAD --format=%B | grep -i 'co-authored-by'`.

Expected: author and committer are both `cnwenf <cnwenf@outlook.com>` and the trailer scan has no output.

- [ ] **Step 4: Push only the existing PR branch and verify GitHub state**

Run: `git push origin HEAD:agent/mesh/d609122f`

Expected: a fast-forward update of PR #111. Query PR #111 afterward and record the new head SHA, mergeability, and automatically triggered checks; do not manually dispatch CI.

