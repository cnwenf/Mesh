/**
 * shell/hooks — 壳层公共 hook 桶导出。
 *
 * G19 动态标签页标题(MES-127 批次④):变参语义段便捷层,写入经权威 hook(src/hooks/useDocumentTitle)。
 */
export { useDocumentTitle, composeDocumentTitle, APP_TITLE_SUFFIX } from './useDocumentTitle';
