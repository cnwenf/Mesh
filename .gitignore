# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment & secrets — never commit
.env
.env.*
*.pem
*.key

# OS / editor
.DS_Store
.idea/
.vscode/

# Sample env template is safe to commit (no real secrets)
!.env.example

# Coverage artifacts
.coverage
.coverage.*
htmlcov/

# Node — never commit dependencies or build caches
node_modules/
