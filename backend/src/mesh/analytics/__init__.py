"""Analytics module (analytics.md): read-only aggregation over source tables.

This package never writes source tables; it only computes metrics and
(optionally) accelerates them via the ``analytics_snapshots`` cache.
"""
